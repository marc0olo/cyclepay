{
  "name": "cyclepay-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:fixtures": "CYCLEPAY_FIXTURES=1 vite build --outDir dist-fixtures",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@icp-sdk/auth": "^8.0.3",
    "@icp-sdk/core": "^5.3.0"
  },
  "devDependencies": {
    "@icp-sdk/bindgen": "^0.4.0",
    "@types/node": "^26.2.0",
    "jsdom": "^29.1.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
