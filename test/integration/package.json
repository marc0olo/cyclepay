{
  "name": "cyclepay-integration",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "PocketIC integration suite — the Card-rail go-live bar (spec §9).",
  "scripts": {
    "build:backend": "cd ../.. && mops build",
    "pretest": "npm run build:backend",
    "test": "vitest run"
  },
  "devDependencies": {
    "@dfinity/pic": "0.22.0",
    "@types/node": "^25.9.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
