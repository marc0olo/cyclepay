{
  "name": "cyclepay-integration",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "PocketIC integration suite \u2014 the Card and ck-USDC go-live bars (spec \u00a79).",
  "scripts": {
    "build:backend": "cd ../.. && mops build",
    "fetch:wasm": "node scripts/fetch-wasms.mjs",
    "pretest": "npm run fetch:wasm && npm run build:backend",
    "test": "vitest run"
  },
  "devDependencies": {
    "@dfinity/pic": "0.22.0",
    "@types/node": "^25.9.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
