Read and follow the instructions in `AGENTS.md`.

`AGENTS.md` is the single agent-instruction file for this repo — ICP skills
setup, project conventions, the issue tracker, triage labels, and the
verification gate all live there. This file exists only to point Claude Code at
it, so that agent instructions never drift between two copies.
