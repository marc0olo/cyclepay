# Progress log — one entry per loop iteration, newest last.

## 2026-06-10 — Iteration 1 — Task 0: Scaffold (DONE)
No PRD or progress.txt existed; bootstrapped both. Created PRD.md with the full
prioritized task breakdown (tasks 0–18, M0–M3) derived from
design-docs/ONCHAIN_GATEWAY_SPEC.md v2.1. Scaffolded the buildable project:
- mops.toml: moc 1.9.0 + lintoko 0.10.0 pinned, core 2.5.0, test 2.1.2 (dev),
  style warnings -W=M0223,M0236,M0237, [canisters.backend], [build] --release.
  NOTE: deliberately NOT using --default-persistent-actors; actors write
  `persistent` explicitly so the icp-cli recipe (which ignores [moc].args)
  compiles identically.
- icp.yaml: backend via @dfinity/motoko@v4.1.0 recipe; local network with
  gateway port 0 (avoids port collisions).
- src/backend/Main.mo: minimal `persistent actor CyclesGateway` + health query.
- test/smoke.test.mo: harness smoke test.
- .gitignore: .icp/cache/, src/backend/dist/. mops.lock committed (spec §8).
Toolchain installed in sandbox: npm -g ic-mops @icp-sdk/icp-cli @icp-sdk/ic-wasm
(+ apt libdbus-1-3 for icp-cli). Verified: mops check ✓, mops test ✓ (1 pass),
mops build ✓, icp build ✓.
NEXT: Task 1 — Types.mo + Orders.mo (Owner variant seam, order state machine,
locked cycle quantity, transition unit tests).

## 2026-06-10 — Iteration 2 — Task 1: Core types + Order state machine (DONE)
- src/backend/Types.mo: Owner = {#ii : Principal} (binding seam §11.1.1) +
  isOwnedBy (pattern-match authz), Rail {#card; #ckUsdc}, Account (ICRC-1),
  Destination {#canister; #cyclesLedgerAccount}, OrderStatus (8 states),
  statusToText, Order (immutable record, lockedCycles quantity per §3),
  TransferIntent (§5.1 write-intent args), JournalEntry (§4.2).
- src/backend/Orders.mo: Store {orders; principalsToOrders}; isLegalTransition
  = §4 diagram, 11 legal edges (incl. Minting→ErrorQueue stale-intent §5.1 and
  IcpAtCMC→ErrorQueue failed-forward Type 2 §4.1); Delivered/ErrorQueue
  terminal; pure transition() → updated copy; create() takes owner as a
  PARAMETER (seam §11.1.3), locks cycle quantity, rejects duplicate id;
  applyTransition, get/getOwned (caller==owner authz), ordersFor history.
  Expiry policy NOT in the core (seam §11.1.4 — per-rail, lands with rails).
- test/orders.test.mo: exhaustive 64-pair matrix as data from the spec table,
  count pinned to 11, terminality check, transition/copy semantics, dup-id,
  authz, history ordering. 80 tests green.
- Gotchas hit: extension methods need the module imported even for type
  aliases — Map<Text,_> implicit compare needs `import Text`, iterator
  .filterMap needs `import Iter` and BOTH type args (<OrderId, Order>).
- Verified: mops check ✓ (lint clean), mops test ✓ (80), mops build ✓,
  icp build ✓. PRD task 1 → ☑, changelog updated.
NEXT: Task 2 — Idempotency.mo + ErrorQueue.mo (per-rail dedup sets w/ 7-day
Stripe pruning, bounded Type1/Type2 error queue, audit-log ring buffer, tests).
## 2026-06-10 — Iteration 3 — Task 2: Idempotency + ErrorQueue + AuditLog (DONE)
- src/backend/Idempotency.mo: stripeEvents/stripeIntents Map<Text,Int>
  (first-seen ns; NOT refreshed on replay so day-6 redelivery still prunes
  day 7), ckUsdcBlocks Set<Nat> never pruned (§4.2 retention); record* → Bool
  (false = duplicate, ack-and-drop); pruneStripe (cutoff: age >= 7d, constant
  604_800_000_000_000 — module-level lets must be STATIC, M0014 rejects
  7*24*60*60*1e9 arithmetic).
- src/backend/ErrorQueue.mo: Kind = #duplicate{orderId;paymentRef} |
  #unattributed{claimedRef;paymentRef} (Type 1) | #undeliverable{orderId;
  cycles} (Type 2) — payloads make types structural; isType1/paymentRefOf;
  Store {entries: Map<Nat,Entry>; var nextId} (monotonic id = age order);
  add(capacity) evicts oldest-resolved-first, oldest-unresolved last resort,
  returns evicted[] for caller to audit-log; resolve (notFound/
  alreadyResolved), resolveByPaymentRef = charge.refunded auto-resolve
  (skips resolved + Type 2); unresolved/all/get/size. Capacity is a call-site
  param — config lands in Main.mo (task 6).
- src/backend/AuditLog.mo: Queue-backed ring buffer (pushBack/popFront O(1)),
  hard cap, monotonic never-reused seq → readers detect drops.
- Tests: idempotency (11), errorqueue (10), auditlog (3) — boundary at
  exactly-7d pruned / 7d-1ns kept, namespace independence, eviction
  preference, id non-reuse, auto-resolve idempotence. 105 total green.
- Gotchas: M0014 (static module lets); Set.insert/Map.insert deprecated M0235
  → use contains+add; .toArray() on iter needs `import Iter` (again).
- Verified: mops check ✓, mops test ✓ (105), mops build ✓, icp build ✓.
NEXT: Task 3 — HMAC-SHA256 + Stripe signature verify (constant-time compare,
Stripe-Signature header parse, timestamp window; known-vector tests).
## 2026-06-10 — Iteration 4 — Task 3: HMAC-SHA256 + Stripe signature (DONE)
- Dep: sha2@0.2.1 (research-ag) — matches core 2.5.0 exactly. REJECTED the
  `hmac` mops pkg (edjCase): pins core@1.0.0 + sha2@0.1.6, would put two core
  versions in the build. HMAC is ~25 lines over a SHA-256 digest, hand-rolled.
- src/backend/Hmac.mo: RFC 2104 (key > 64B → hashed, zero-pad, ipad/opad),
  multi-part message arg so Card can MAC "<t>." + body without a copy;
  constantTimeEqual = XOR-OR over all bytes (compare is on the forge path:
  HMAC verify == forge), early return on length mismatch only.
- src/backend/Util.mo: hex codec (lowercase encode, either-case decode,
  null on odd len / bad char).
- src/backend/rails/Card.mo: parseSignatureHeader (Stripe reference-parser
  semantics: first t= wins, v0=/junk ignored, v1= filtered to valid 32-byte
  hex; null if no usable t/v1), signedPayloadMac("<t>.<raw body>"), verify =
  parse → ABSOLUTE window |now−t| ≤ tolerance (default 300s; t is signed so
  it can't be forged to beat the window) → constant-time compare vs EVERY v1
  (rotation). Errors: #malformedHeader / #timestampOutsideWindow{t,now} /
  #signatureMismatch.
- Tests pinned to EXTERNAL vectors (python hmac, computed this iteration, in
  shell history not committed): RFC 4231 TC1–4,6,7; empty/64B/65B key
  boundaries; crafted Stripe header vector + rotated-secret second v1.
  Window boundary at exactly ±tolerance vs +1s. 18 card + 20 hmac = 38 new,
  143 total green.
- Gotchas: none at compile (first-try clean — parenthesize variant payloads
  `#tag({...})`); `mops test` only prints per-FILE ✓ — run
  `mops test <name>` to see per-test counts.
NEXT: Task 4 — hand-rolled Http.mo (HttpRequest parse, case-insensitive
header lookup, query-string strip, body-size guard, route table w/ upgrade
flag, POST /webhook/stripe entry; parser+routing unit tests).
## 2026-06-10 — Iteration 5 — Task 4: Hand-rolled Http.mo (DONE)
- src/backend/Http.mo: gateway Request/Response (certificate_version omitted,
  Candid subtyping drops it); pathOf (strip from first '?'); headerValue
  (ASCII-fold names only, first match wins — core Text has NO toLower, hand-
  rolled via Char.fromNat32(+32)); dispatch off [Route] table w/ per-route
  upgrade flag (seam §11.1.2). 404 unknown path / 405 + Allow header (methods
  on that path, table order) / 413 over body cap BEFORE the upgrade decision
  (oversize never reaches consensus). Query half: upgrade route → ?true w/o
  running handler; non-upgrade route runs in the query. Update half re-applies
  all guards (http_request_update is directly Candid-callable).
- src/backend/Main.mo: http_request(_update) wired; one-entry route table
  POST /webhook/stripe upgrade=true; handler = 503 stub (Stripe retries
  non-2xx) until tasks 5+8. routes AND 64 KiB cap are `transient` — closures
  aren't stable; persistent let would freeze first-deploy config forever.
- test/http.test.mo: 25 tests — pathOf edges (first-? wins, empty url),
  header case/dup/missing, at-cap vs over-cap boundary, handler-not-run on
  query upgrade (stub returns 299; seeing 200 proves skip), echo handler
  proves update dispatch passes the req, exact-case method match, 405 Allow
  content. 168 total green.
- Gotchas: mo:core Text has no toLower/toUpper (ASCII fold by hand); lint
  M0236 wants dot notation even for Text.encodeUtf8 → "x".encodeUtf8().
- Verified: mops check ✓, mops test ✓ (168), mops build ✓, icp build ✓,
  generated .did matches HTTP-gateway protocol.
NEXT: Task 5 — Auth.mo + Secret.mo (controller allowlist, reject anonymous
on user API, admin-set/rotate webhook secret w/ SEV-SNP posture note; tests).
## 2026-06-10 — Iteration 6 — Task 5: Auth + Secret (DONE)
- src/backend/Auth.mo: §7 flat allowlist = the CANISTER CONTROLLER SET
  (OR-semantics, equal privileges; edits via canister settings, M-of-N =
  multisig-canister controller). checkAdmin(caller, isController) — the
  controller check is an INJECTED PREDICATE so the module unit-tests without
  an IC env; Main passes Principal.isController (exists in core 2.5.0,
  works in query context). Anonymous rejected BEFORE the predicate.
  checkUser = II user-API gate (wired in task 6; webhook route stays
  anonymous-by-design, payload-authed).
- src/backend/Secret.mo: plaintext-by-design store (SEV-SNP posture doc'd
  on the module: checkpoints/state-sync caveat, boundary-node provisioning
  exposure, burn-cap backstop). Holds UTF-8 of the FULL whsec_ string
  (prefix included = HMAC key per Stripe reference verifiers) + setAtNs +
  generation (ops confirm rotation w/o read-back); set rejects <16 bytes,
  store untouched on #err. No dual-secret window: Stripe sends one v1= per
  active secret during overlap, Card.verify accepts any match.
- Main.mo: persistent webhookSecret; requireAdmin TRAPS; set_webhook_secret
  (Text→Result<(), SetError>); webhook_secret_status query (admin-gated,
  never returns the secret). Webhook handler stays 503 stub until task 8.
- Tests: auth (6) + secret (7) = 13 new, 181 total green. Boundary at
  exactly-16 vs 15 bytes; anonymous-beats-predicate; rejected rotation
  preserves working secret.
- Gotchas: core 2.5.0 Principal has NO fromBlob — test principals must be
  valid fromText strings ("aaaaa-aa", "2ibo7-dia" both fine).
- Verified: mops check ✓, mops test ✓ (181), mops build ✓, icp build ✓,
  .did gains exactly the two admin methods.
NEXT: Task 6 — Order Candid API in Main.mo (create_order w/ raw_rand id +
client_reference_id "<principal>_<orderId>", get_order/history authz,
fixed card tiers config; wires Auth.checkUser + Orders store).
## 2026-06-10 — Iteration 7 — Task 6: Order Candid API (DONE)
- src/backend/Tiers.mo: fixed card tiers = operator config {id; usdCents;
  paymentLinkUrl} (links made in Stripe Dashboard, registered via admin
  call); validate (unique non-empty ids, non-zero cents) + find.
- src/backend/Orders.mo: idFromEntropy (first 16 raw_rand bytes → 32-char
  lowercase hex, null if <16B = broken source); clientReferenceId =
  "<principal>_<orderId>" (principal is [a-z0-9-], id is hex → single '_'
  splits unambiguously; task 8 parses it claimed-not-trusted).
- src/backend/Main.mo: persistent orderStore + cardTiers (empty by default —
  no invented prices); create_order = checkUser → tier find → quote →
  raw_rand loop (≤3 re-draws on dup id) → Orders.create with #ii(caller)
  owner at the API edge (seam §11.1.3); tier+quote read BEFORE the await so
  config swaps can't mix pricings. PRICING SEAM FAILS CLOSED: transient
  quoteCyclesForUsdCents stub returns null until Forex (task 7) →
  #rateUnavailable (§3.1 — same answer as a real stale-rate outage).
  get_order/list_orders (getOwned/ordersFor; anonymous owns nothing, no
  existence leak), set_card_tiers (admin, atomic validate), card_tiers query.
- Tests: tiers (7) + orders id/clientRef suites (6) = 13 new, 194 total.
- Gotchas: `let ? ... else assert false` doesn't compile — else branch must
  DIVERGE; use `else Runtime.trap(...)`. Dot-notation lint (M0236) fires on
  Array.sliceToArray/find too: `entropy.toArray().sliceToArray(0, n)`,
  `tiers.find(...)` (module import still required).
- Verified: mops check ✓ (lint clean), mops test ✓ (194), mops build ✓,
  icp build ✓, .did gains exactly the 5 order/tier methods.
NEXT: Task 7 — Forex.mo (§3.1): USD↔XDR HTTPS outcall w/ coarse-rounding
transform, stable {rate, ts} cache, lazy refresh, single-flight, retry cap,
fail-closed wiring into quoteCyclesForUsdCents; fee formula (≈2.9% + $0.30)
net-of-fees pricing; mocked-outcall unit tests.
