# Progress log — one entry per loop iteration, newest last.
#
# FROZEN (2026-06-10) — historical artifact. Tracking moved to GitHub
# Issues (the source of truth; see docs/agents/issue-tracker.md). Do not
# append entries here — comment on the relevant issue instead.

## 2026-06-10 — Iteration 1 — Task 0: Scaffold (DONE)
No PRD or progress.txt existed; bootstrapped both. Created PRD.md with the full
prioritized task breakdown (tasks 0–18, M0–M3) derived from
design-docs/ONCHAIN_GATEWAY_SPEC.md v2.1. Scaffolded the buildable project:
- mops.toml: moc 1.9.0 + lintoko 0.10.0 pinned, core 2.5.0, test 2.1.2 (dev),
  style warnings -W=M0223,M0236,M0237, [canisters.backend], [build] --release.
  NOTE: deliberately NOT using --default-persistent-actors; actors write
  `persistent` explicitly so the icp-cli recipe (which ignores [moc].args)
  compiles identically.
- icp.yaml: backend via @dfinity/motoko@v4.1.0 recipe; local network with
  gateway port 0 (avoids port collisions).
- src/backend/Main.mo: minimal `persistent actor CyclesGateway` + health query.
- test/smoke.test.mo: harness smoke test.
- .gitignore: .icp/cache/, src/backend/dist/. mops.lock committed (spec §8).
Toolchain installed in sandbox: npm -g ic-mops @icp-sdk/icp-cli @icp-sdk/ic-wasm
(+ apt libdbus-1-3 for icp-cli). Verified: mops check ✓, mops test ✓ (1 pass),
mops build ✓, icp build ✓.
NEXT: Task 1 — Types.mo + Orders.mo (Owner variant seam, order state machine,
locked cycle quantity, transition unit tests).

## 2026-06-10 — Iteration 2 — Task 1: Core types + Order state machine (DONE)
- src/backend/Types.mo: Owner = {#ii : Principal} (binding seam §11.1.1) +
  isOwnedBy (pattern-match authz), Rail {#card; #ckUsdc}, Account (ICRC-1),
  Destination {#canister; #cyclesLedgerAccount}, OrderStatus (8 states),
  statusToText, Order (immutable record, lockedCycles quantity per §3),
  TransferIntent (§5.1 write-intent args), JournalEntry (§4.2).
- src/backend/Orders.mo: Store {orders; principalsToOrders}; isLegalTransition
  = §4 diagram, 11 legal edges (incl. Minting→ErrorQueue stale-intent §5.1 and
  IcpAtCMC→ErrorQueue failed-forward Type 2 §4.1); Delivered/ErrorQueue
  terminal; pure transition() → updated copy; create() takes owner as a
  PARAMETER (seam §11.1.3), locks cycle quantity, rejects duplicate id;
  applyTransition, get/getOwned (caller==owner authz), ordersFor history.
  Expiry policy NOT in the core (seam §11.1.4 — per-rail, lands with rails).
- test/orders.test.mo: exhaustive 64-pair matrix as data from the spec table,
  count pinned to 11, terminality check, transition/copy semantics, dup-id,
  authz, history ordering. 80 tests green.
- Gotchas hit: extension methods need the module imported even for type
  aliases — Map<Text,_> implicit compare needs `import Text`, iterator
  .filterMap needs `import Iter` and BOTH type args (<OrderId, Order>).
- Verified: mops check ✓ (lint clean), mops test ✓ (80), mops build ✓,
  icp build ✓. PRD task 1 → ☑, changelog updated.
NEXT: Task 2 — Idempotency.mo + ErrorQueue.mo (per-rail dedup sets w/ 7-day
Stripe pruning, bounded Type1/Type2 error queue, audit-log ring buffer, tests).
## 2026-06-10 — Iteration 3 — Task 2: Idempotency + ErrorQueue + AuditLog (DONE)
- src/backend/Idempotency.mo: stripeEvents/stripeIntents Map<Text,Int>
  (first-seen ns; NOT refreshed on replay so day-6 redelivery still prunes
  day 7), ckUsdcBlocks Set<Nat> never pruned (§4.2 retention); record* → Bool
  (false = duplicate, ack-and-drop); pruneStripe (cutoff: age >= 7d, constant
  604_800_000_000_000 — module-level lets must be STATIC, M0014 rejects
  7*24*60*60*1e9 arithmetic).
- src/backend/ErrorQueue.mo: Kind = #duplicate{orderId;paymentRef} |
  #unattributed{claimedRef;paymentRef} (Type 1) | #undeliverable{orderId;
  cycles} (Type 2) — payloads make types structural; isType1/paymentRefOf;
  Store {entries: Map<Nat,Entry>; var nextId} (monotonic id = age order);
  add(capacity) evicts oldest-resolved-first, oldest-unresolved last resort,
  returns evicted[] for caller to audit-log; resolve (notFound/
  alreadyResolved), resolveByPaymentRef = charge.refunded auto-resolve
  (skips resolved + Type 2); unresolved/all/get/size. Capacity is a call-site
  param — config lands in Main.mo (task 6).
- src/backend/AuditLog.mo: Queue-backed ring buffer (pushBack/popFront O(1)),
  hard cap, monotonic never-reused seq → readers detect drops.
- Tests: idempotency (11), errorqueue (10), auditlog (3) — boundary at
  exactly-7d pruned / 7d-1ns kept, namespace independence, eviction
  preference, id non-reuse, auto-resolve idempotence. 105 total green.
- Gotchas: M0014 (static module lets); Set.insert/Map.insert deprecated M0235
  → use contains+add; .toArray() on iter needs `import Iter` (again).
- Verified: mops check ✓, mops test ✓ (105), mops build ✓, icp build ✓.
NEXT: Task 3 — HMAC-SHA256 + Stripe signature verify (constant-time compare,
Stripe-Signature header parse, timestamp window; known-vector tests).
## 2026-06-10 — Iteration 4 — Task 3: HMAC-SHA256 + Stripe signature (DONE)
- Dep: sha2@0.2.1 (research-ag) — matches core 2.5.0 exactly. REJECTED the
  `hmac` mops pkg (edjCase): pins core@1.0.0 + sha2@0.1.6, would put two core
  versions in the build. HMAC is ~25 lines over a SHA-256 digest, hand-rolled.
- src/backend/Hmac.mo: RFC 2104 (key > 64B → hashed, zero-pad, ipad/opad),
  multi-part message arg so Card can MAC "<t>." + body without a copy;
  constantTimeEqual = XOR-OR over all bytes (compare is on the forge path:
  HMAC verify == forge), early return on length mismatch only.
- src/backend/Util.mo: hex codec (lowercase encode, either-case decode,
  null on odd len / bad char).
- src/backend/rails/Card.mo: parseSignatureHeader (Stripe reference-parser
  semantics: first t= wins, v0=/junk ignored, v1= filtered to valid 32-byte
  hex; null if no usable t/v1), signedPayloadMac("<t>.<raw body>"), verify =
  parse → ABSOLUTE window |now−t| ≤ tolerance (default 300s; t is signed so
  it can't be forged to beat the window) → constant-time compare vs EVERY v1
  (rotation). Errors: #malformedHeader / #timestampOutsideWindow{t,now} /
  #signatureMismatch.
- Tests pinned to EXTERNAL vectors (python hmac, computed this iteration, in
  shell history not committed): RFC 4231 TC1–4,6,7; empty/64B/65B key
  boundaries; crafted Stripe header vector + rotated-secret second v1.
  Window boundary at exactly ±tolerance vs +1s. 18 card + 20 hmac = 38 new,
  143 total green.
- Gotchas: none at compile (first-try clean — parenthesize variant payloads
  `#tag({...})`); `mops test` only prints per-FILE ✓ — run
  `mops test <name>` to see per-test counts.
NEXT: Task 4 — hand-rolled Http.mo (HttpRequest parse, case-insensitive
header lookup, query-string strip, body-size guard, route table w/ upgrade
flag, POST /webhook/stripe entry; parser+routing unit tests).
## 2026-06-10 — Iteration 5 — Task 4: Hand-rolled Http.mo (DONE)
- src/backend/Http.mo: gateway Request/Response (certificate_version omitted,
  Candid subtyping drops it); pathOf (strip from first '?'); headerValue
  (ASCII-fold names only, first match wins — core Text has NO toLower, hand-
  rolled via Char.fromNat32(+32)); dispatch off [Route] table w/ per-route
  upgrade flag (seam §11.1.2). 404 unknown path / 405 + Allow header (methods
  on that path, table order) / 413 over body cap BEFORE the upgrade decision
  (oversize never reaches consensus). Query half: upgrade route → ?true w/o
  running handler; non-upgrade route runs in the query. Update half re-applies
  all guards (http_request_update is directly Candid-callable).
- src/backend/Main.mo: http_request(_update) wired; one-entry route table
  POST /webhook/stripe upgrade=true; handler = 503 stub (Stripe retries
  non-2xx) until tasks 5+8. routes AND 64 KiB cap are `transient` — closures
  aren't stable; persistent let would freeze first-deploy config forever.
- test/http.test.mo: 25 tests — pathOf edges (first-? wins, empty url),
  header case/dup/missing, at-cap vs over-cap boundary, handler-not-run on
  query upgrade (stub returns 299; seeing 200 proves skip), echo handler
  proves update dispatch passes the req, exact-case method match, 405 Allow
  content. 168 total green.
- Gotchas: mo:core Text has no toLower/toUpper (ASCII fold by hand); lint
  M0236 wants dot notation even for Text.encodeUtf8 → "x".encodeUtf8().
- Verified: mops check ✓, mops test ✓ (168), mops build ✓, icp build ✓,
  generated .did matches HTTP-gateway protocol.
NEXT: Task 5 — Auth.mo + Secret.mo (controller allowlist, reject anonymous
on user API, admin-set/rotate webhook secret w/ SEV-SNP posture note; tests).
## 2026-06-10 — Iteration 6 — Task 5: Auth + Secret (DONE)
- src/backend/Auth.mo: §7 flat allowlist = the CANISTER CONTROLLER SET
  (OR-semantics, equal privileges; edits via canister settings, M-of-N =
  multisig-canister controller). checkAdmin(caller, isController) — the
  controller check is an INJECTED PREDICATE so the module unit-tests without
  an IC env; Main passes Principal.isController (exists in core 2.5.0,
  works in query context). Anonymous rejected BEFORE the predicate.
  checkUser = II user-API gate (wired in task 6; webhook route stays
  anonymous-by-design, payload-authed).
- src/backend/Secret.mo: plaintext-by-design store (SEV-SNP posture doc'd
  on the module: checkpoints/state-sync caveat, boundary-node provisioning
  exposure, burn-cap backstop). Holds UTF-8 of the FULL whsec_ string
  (prefix included = HMAC key per Stripe reference verifiers) + setAtNs +
  generation (ops confirm rotation w/o read-back); set rejects <16 bytes,
  store untouched on #err. No dual-secret window: Stripe sends one v1= per
  active secret during overlap, Card.verify accepts any match.
- Main.mo: persistent webhookSecret; requireAdmin TRAPS; set_webhook_secret
  (Text→Result<(), SetError>); webhook_secret_status query (admin-gated,
  never returns the secret). Webhook handler stays 503 stub until task 8.
- Tests: auth (6) + secret (7) = 13 new, 181 total green. Boundary at
  exactly-16 vs 15 bytes; anonymous-beats-predicate; rejected rotation
  preserves working secret.
- Gotchas: core 2.5.0 Principal has NO fromBlob — test principals must be
  valid fromText strings ("aaaaa-aa", "2ibo7-dia" both fine).
- Verified: mops check ✓, mops test ✓ (181), mops build ✓, icp build ✓,
  .did gains exactly the two admin methods.
NEXT: Task 6 — Order Candid API in Main.mo (create_order w/ raw_rand id +
client_reference_id "<principal>_<orderId>", get_order/history authz,
fixed card tiers config; wires Auth.checkUser + Orders store).
## 2026-06-10 — Iteration 7 — Task 6: Order Candid API (DONE)
- src/backend/Tiers.mo: fixed card tiers = operator config {id; usdCents;
  paymentLinkUrl} (links made in Stripe Dashboard, registered via admin
  call); validate (unique non-empty ids, non-zero cents) + find.
- src/backend/Orders.mo: idFromEntropy (first 16 raw_rand bytes → 32-char
  lowercase hex, null if <16B = broken source); clientReferenceId =
  "<principal>_<orderId>" (principal is [a-z0-9-], id is hex → single '_'
  splits unambiguously; task 8 parses it claimed-not-trusted).
- src/backend/Main.mo: persistent orderStore + cardTiers (empty by default —
  no invented prices); create_order = checkUser → tier find → quote →
  raw_rand loop (≤3 re-draws on dup id) → Orders.create with #ii(caller)
  owner at the API edge (seam §11.1.3); tier+quote read BEFORE the await so
  config swaps can't mix pricings. PRICING SEAM FAILS CLOSED: transient
  quoteCyclesForUsdCents stub returns null until Forex (task 7) →
  #rateUnavailable (§3.1 — same answer as a real stale-rate outage).
  get_order/list_orders (getOwned/ordersFor; anonymous owns nothing, no
  existence leak), set_card_tiers (admin, atomic validate), card_tiers query.
- Tests: tiers (7) + orders id/clientRef suites (6) = 13 new, 194 total.
- Gotchas: `let ? ... else assert false` doesn't compile — else branch must
  DIVERGE; use `else Runtime.trap(...)`. Dot-notation lint (M0236) fires on
  Array.sliceToArray/find too: `entropy.toArray().sliceToArray(0, n)`,
  `tiers.find(...)` (module import still required).
- Verified: mops check ✓ (lint clean), mops test ✓ (194), mops build ✓,
  icp build ✓, .did gains exactly the 5 order/tier methods.
NEXT: Task 7 — Forex.mo (§3.1): USD↔XDR HTTPS outcall w/ coarse-rounding
transform, stable {rate, ts} cache, lazy refresh, single-flight, retry cap,
fail-closed wiring into quoteCyclesForUsdCents; fee formula (≈2.9% + $0.30)
net-of-fees pricing; mocked-outcall unit tests.
## 2026-06-10 — Iteration 8 — Task 7: Forex subsystem (DONE)
- Dep: ic@4.0.0 (caffeinelabs) — shares core@2.5.0 exactly (no version
  split); Call.httpRequest auto-attaches outcall cycles (ic0 cost API).
- src/backend/Forex.mo (pure half — unit-testable w/ mocked Blob bodies):
  rate = XDR-per-USD micros (1 XDR = 1T cycles ⇒ cycles = net·micros·1e4);
  default source open.er-api.com/v6/latest/USD (keyless, USD-base);
  extractXdrPerUsdMicros (first "XDR" key wins, >6 frac digits truncated);
  coarseRound to 1_000 micros (~0.14%); plausibility band 0.1–10 XDR/USD
  (decimal-point bug at source can't mint free/1000x orders); transformBody
  → canonical micros text, ANY failure → empty body (consensus on failure
  too); parseCanonicalMicros re-applies band; fee = ⌈gross·bps/1e4⌉+fixed
  (ceil: overestimate fee, never over-deliver; net>0); quote = one snapshot
  of config+rate → #ok/#stale/#unpriceable; hostOf for Host header.
- Main.mo: persistent forexCache + forexConfig (290bps/30¢/1h, admin
  set_forex_config validated atomically, public forex_status); TRANSIENT
  single-flight flag (persistent flag stuck by mid-outcall upgrade would
  deadlock forever) w/ try/finally reset; retry cap 3; max_response_bytes
  16k (charged on ceiling); forex_transform query; create_order quotes
  cache → #stale → refresh → requote → fail-closed #rateUnavailable; new
  #tierBelowFees error (config problem ≠ rate outage).
- Tests: 43 new (parser matrix, band/round boundaries incl. round-into-band,
  fee vectors cross-checked w/ python, exactly-at-window staleness, quote,
  config validation). 237 total green.
- Gotchas: core Char has NO toNat — use c.toNat32().toNat(); encodeUtf8 dot
  notation in TESTS needs `import Text` too (M0072, same old story);
  try/finally works fine in moc 1.9 for the single-flight reset.
- Verified: mops check ✓ (lint clean), mops test ✓ (237), mops build ✓,
  icp build ✓, .did gains forex_status/forex_transform/set_forex_config.
NEXT: Task 8 — Stripe webhook ingestion (rails/Card.mo complete): parse
checkout.session.completed + charge.refunded JSON, claimed-not-trusted
client_reference_id resolution, event.id+payment_intent dedup, honor actual
paid amount → Paid; unmatched/dup → Type 1; refund auto-resolve; crafted
signed-payload tests.
## 2026-06-10 — Iteration 9 — Task 8: Stripe webhook ingestion (DONE)
RESUMED IN-FLIGHT WORK: a previous run left the full implementation
uncommitted (Json.mo, Card.mo ingestion, Main.mo wiring, Pricing snapshot,
markPaid, parseClientReferenceId) but stopped before tests/PRD/commit.
This iteration reviewed that code (matches spec §4.1/§6.1), fixed the two
test files its signature changes broke, and wrote all tests.
- src/backend/Json.mo (was untracked): strict JSON tree parser — mops json
  pkg rejected (deps deprecated base; same split-rejection as hmac task 3).
  Tree-parse not key-scan: verified bodies still carry attacker-influenced
  STRING VALUES. Raw number lexemes, integer-only asNat; lone surrogates/
  control chars/trailing garbage fail; depth cap 64.
- Card.parseEvent: checkoutCompleted{eventId;paymentIntent;clientReferenceId
  (JSON null = absent);amountTotalCents;currency;paid} | chargeRefunded |
  unhandled (acked); missing required field → 400 (subscription-mode null
  payment_intent stays visible in Stripe dashboard).
- Card.handleWebhook (sync end-to-end, Deps-injected stores): secret null →
  503; verify → prune → route. Checkout: event.id dedup → payment_status
  (unpaid acks WITHOUT consuming intent) → intent dedup → attribution
  (parseClientReferenceId: claimed principal compared as TEXT — fromText on
  garbage traps → 5xx retry loop; owner/rail/currency checked) → Type 1
  #unattributed on failure (200 — handled BY THE OPERATOR); fresh intent on
  handled order → Type 1 #duplicate; actual amount honored: tier match =
  locked qty verbatim, mismatch repriced from order.pricing snapshot
  (Types.Pricing captured at create; Orders.markPaid swaps honored qty),
  below-fee-floor → Type 1. charge.refunded auto-resolves by intent.
- Forex.netCents widened to {feeBps;feeFixedCents}; Forex.quote #ok now
  {cycles; xdrPerUsdMicros} so Main.quoteTier persists snapshot+quote from
  one epoch. Main.mo: persistent dedup/errorQueue/auditLog + error_queue/
  resolve_error/audit_log admin methods.
- Tests: json (19), webhook (30, end-to-end over SIGNED crafted payloads —
  signedPayloadMac is pinned to external vectors in card.test.mo so this
  isn't self-agreement), orders +9 (parseClientReferenceId, markPaid);
  fixed forex.test.mo quote shape + orders.test.mo create arity. 295 green.
- Gotchas: none new — first-try clean compile on all three new test files
  (the iteration-7 lesson list held: parenthesized variant payloads, module
  imports for dot-notation, record-update `{ x with field = v }` syntax).
- Verified: mops check ✓ (lint clean), mops test ✓ (295), mops build ✓,
  icp build ✓, .did gains exactly error_queue/resolve_error/audit_log.
NEXT: Task 9 — CMC mint pipeline (Cmc.mo, §5/§5.1): candid bindings for ICP
ledger + CMC, write-intent-before-call w/ created_at_time dedup,
icrc1_transfer → block_index → notify_top_up, mint-to-self-then-forward,
failed forward → Type 2, CMC rate staleness guard; intent/replay unit tests.
## 2026-06-10 — Iteration 10 — Task 9: CMC mint pipeline (DONE)
- src/backend/Cmc.mo (pure half, unit-testable): ledger/CMC/cycles-ledger
  Candid types; topUpSubaccount (len-prefixed principal, 32B) + TPUP memo
  (LE 0x50555054) pinned to python-computed vectors; e8s = ceil(cycles /
  xdr_permyriad_per_icp) (1 e8s mints exactly `permyriad` cycles; round UP
  so the mint covers the locked qty, dust is operator-side); freshCmcRate
  15-min staleness guard (age >= window = stale, house convention) + zero-
  rate refusal; buildIntent/transferArgs = deterministic §5.1 write-intent
  (replay is bit-identical BY CONSTRUCTION — first attempt and recovery are
  the same #replayTransfer stage); interpretTransfer (#Ok AND #Duplicate
  both recover block_index; retriable = nothing-recorded vs escalate =
  replay-can-never-succeed); interpretNotify; journal Map + patch helper
  (no-op on missing id — never trap mid-money-flow); stageOf = the WHOLE
  §5.1/§5.2 resume decision as one pure function (incl. maxRetries cap for
  the notify loop the 24h window doesn't bound).
- DECISION: one notify path — mint-to-self-then-forward via notify_top_up
  (self)+TPUP for BOTH destination kinds; notify_mint_cycles/MINT unused
  (would strand value in the cycles-LEDGER balance, splitting Type 2's
  "cycles in app balance" invariant). Forward: #canister → ic.deposit_cycles
  w/ attached cycles; #cyclesLedgerAccount → cycles-ledger deposit.
- DECISION: forward is AT-MOST-ONCE — cyclesMinted doubles as a pre-forward
  marker committed before the forward await; death mid-forward resumes as
  #ambiguousForward → escalate (operator checks destination), never auto-
  re-forward. Failed (not ambiguous) forward = clean Type 2 (deposit
  rejection auto-refunds attached cycles to app balance).
- ErrorQueue.Kind += #stuckMint{orderId; stage} (§5.1 escalations: stale
  intent/CMC rejection/ambiguous forward) — neither Type 1 nor Type 2, no
  paymentRef so charge.refunded auto-resolve skips it.
- Main.mo: persistent mintJournal; transient per-order single-flight Set +
  maxMintRetries 25; driveMint loop = stageOf-dispatched (begin → rate fetch
  → intent+#minting in ONE sync block → fall through to replayTransfer →
  block+#icpAtCmc sync → notify → marker → forward → #delivered);
  sweepMintable over #paid/#minting/#icpAtCmc (task 11 timer reuses);
  webhook kick = `ignore async` detached self-message after dispatch so
  Stripe's ack never waits on ledger/CMC; admin process_order + mint_journal.
- Tests: cmc (37: pinned vectors, boundaries at exactly-window and
  exactly-24h, full interpret matrices, journal patch, 13-case stageOf incl.
  ambiguity-beats-retries) + errorqueue (+2 stuckMint). 334 total green.
- Gotchas: param named `self` on a module func makes lint M0236 demand dot
  notation at the CALL site — rename the param; `ignore async {...}` inside
  an async public func is the fire-and-forget kick (state already committed
  when the self-message runs); Map/Text imports needed in TESTS for
  journal.get dot notation (same M0072 story as always).
- Verified: mops check ✓ (lint clean), mops test ✓ (334), mops build ✓,
  icp build ✓, .did gains exactly process_order/mint_journal.
NEXT: Task 10 — Treasury.mo (§5.3): ICP float accounting (icrc1_balance_of),
Paid → AwaitingTreasury pre-gate when float short, max-wait → error queue,
low-float soft gate + balance-alert query, per-period rolling ICP burn cap
w/ pause + manual override; unit tests for cap window math.
## 2026-06-10 — Iteration 11 — Task 10: Treasury + burn cap (DONE)
- src/backend/Treasury.mo (pure half, §5.3): Queue-backed rolling burn
  ledger (time-ordered; burnedInWindow = age < window, recordBurn prunes
  front); gate() = the pre-gate, CAP CHECKED BEFORE FLOAT (blast-radius
  bound must hold even with a full float; leaked-secret drains have one),
  proceed iff burned+mint <= cap AND float >= mint+fee; holdStage =
  max-wait (age >= bound escalates); lowFloat/lowFloatSignal (threshold 0
  = disarmed; armed-but-unobserved = low); validateConfig.
- DECISION: default burnCapE8s = 0 — FAIL CLOSED, every mint holds until
  the operator sizes the bound (empty-tiers stance: a default cap in ICP
  is an invented money decision). Cap 0 doubles as the pause lever.
  Defaults: 24h window, 72h max hold, alert disarmed.
- DECISION: cap consumption commits in the SAME §5.1 sync block as the
  intent (after transition, before transfer await) and is never refunded
  on failure — over-counting is the fail-safe direction.
- DECISION: max-wait escalation reuses #stuckMint{stage="treasuryWait
  Exceeded"} (no new ErrorQueue.Kind; doc widened — position is certain
  here: fiat in, nothing minted, operator refunds in Stripe Dashboard).
- Main.mo: driveMint intercepts #awaitingTreasury BEFORE Cmc.stageOf
  (stageOf + its 13-case test matrix untouched) — in-window holds retry
  #begin so the pre-gate is the single decision point; #begin gained the
  balance_of await + observeFloat + gate (re-read status #paid OR
  #awaitingTreasury after awaits; held #paid → #awaitingTreasury w/ audit,
  re-hold silent so updatedAtNs keeps the max-wait clock); sweepMintable
  += #awaitingTreasury; persistent treasuryConfig/burnLedger/
  lastFloatObservation; observeFloat audits only the CROSSING into low;
  admin set_treasury_config/reset_burn_window/refresh_float + public
  treasury_status (frontend soft-gates tiers off lowFloat).
- Tests: treasury (16) — window boundary at exactly-24h, front-prune,
  exact cap/float boundaries, cap-beats-float pin, window-roll frees cap,
  max-wait boundary, signal matrix. 350 total green.
- Gotcha: own test bug, not code — at t=25h a t=1h burn is EXACTLY 24h old
  → pruned (age >= window). First-try clean compile + lint otherwise.
- Verified: mops check ✓ (lint clean), mops test ✓ (350), mops build ✓,
  icp build ✓, .did gains exactly the 4 treasury methods.
NEXT: Task 11 — Recovery timer (§5.2): recurringTimer sweep reusing
sweepMintable, single-flight guard, re-arm in postupgrade (transient timer
id), expiry sweep for stale #created orders if in scope; timer-logic tests.
## 2026-06-10 — Iteration 12 — Task 11: Recovery timer (DONE)
- src/backend/Recovery.mo (pure half): validateInterval = cadence bound
  interval <= ledgerDedupWindow/4 (§5.1 "cadence ≪ 24h" pinned to ≥4 sweeps
  per window — one sweep/window burns the only replay chance on one
  transient failure); defaultIntervalNs 1h (25 maxMintRetries × 1h > 24h,
  pinned by test); isSweepable extracts sweepMintable's inline status
  filter (#paid/#minting/#icpAtCmc/#awaitingTreasury; #created waits on
  the user — expiry stays per-rail per seam §11.1.4, NOT in scope here).
- Main.mo: persistent recoverySweepIntervalNs (operator cadence survives
  upgrades); transient recoverySweepInFlight single-flight (persistent
  flag stuck by mid-sweep upgrade = the §5.2 pumping deadlock) w/
  try/finally; persistent lastRecoverySweep {atNs; pending} liveness
  observation; recoveryTimerId = TRANSIENT INITIALIZER calling
  Timer.recurringTimer<system> — EOP re-runs transient initializers on
  every upgrade, so this IS the postupgrade re-arm (no system func
  needed; IC drops timers on upgrade so nothing stale to cancel).
  Webhook kick deliberately bypasses the sweep flag (in-flight sweep
  enumerated pending before the new order turned #paid; guarded kick
  could cost a paying user a full interval). 24h stale-intent guard
  needed zero new code — Cmc.stageOf (task 9) already escalates.
  Admin set_recovery_interval (validated vs Cmc.ledgerDedupWindowNs,
  cancelTimer + re-arm immediately, audited) + public recovery_status.
- Tests: recovery (8) — zero/min/exactly-window÷4 vs +1ns boundaries,
  default-legal-vs-real-window, 25×default>24h retry-budget pin,
  sweepable matrix pinned exhaustive 4-of-8. 358 total green.
- Gotchas: none — first-try clean compile AND lint. Note import order in
  Main.mo is case-INsensitive by path ("Recovery" sorts before
  "rails/Card"); shared (non-query) funcs have <system> capability so
  set_recovery_interval can re-arm without tricks.
- Verified: mops check ✓ (lint clean), mops test ✓ (358), mops build ✓,
  icp build ✓, .did gains exactly set_recovery_interval/recovery_status.
NEXT: Task 12 — PocketIC integration suite, the Card go-live bar (§9):
real ledger/CMC Wasms, crafted HMAC-signed webhooks, mocked forex outcall,
time control; happy path, duplicate/replay, ambiguous-transfer recovery,
AwaitingTreasury, Type 1/Type 2, forex fail-closed, upgrade-mid-flight,
postupgrade timer re-arm.
## 2026-06-10 — Iteration 13 — Task 12: PocketIC suite (IMPLEMENTED; run pending 4K-page host)
- test/integration/: TS suite, vitest + @dfinity/pic 0.22 (bundles pocket-ic
  server 14). KEY DISCOVERY: server-14 `icpFeatures` deploys the REAL NNS
  ICP ledger + CMC + cycles ledger at mainnet IDs (and funds the anonymous
  principal 1B ICP = our operator float source) — no wasm downloads, no
  init-arg candid, satisfies §9 "real Wasms" with zero drift surface.
  CMC rate driven via governance-impersonated set_icp_xdr_conversion_rate
  (hidden update, caller==governance check only — PocketIC allows any
  sender); MUST re-set after every advanceTime >15min (Cmc.cmcRateMaxAgeNs).
- Files: src/idl.ts (hand-transcribed backend.did factory + icrc1/cmc
  subsets), src/types.ts (typed candid mirrors), src/harness.ts (setup,
  governance/operator helpers, node:crypto Stripe signer, outcall mock via
  deferred-actor submit→mock→resume, tick-stepped status polling),
  src/gateway.spec.ts (15 sequential scenarios, one shared instance).
- Scenario highlights: §3 pricing vector asserted through the real
  forex_transform; exactly-one-float-debit invariant on the happy path AND
  after upgrade-mid-transfer replay (§5.1: tick until #minting commits,
  upgrade kills the callback, advanceTime(1h) fires the re-armed transient
  timer, replay recovers block via dedup — float delta == e8s+fee ONCE);
  upgrade-mid-forward caught between cyclesMinted marker and delivery →
  #ambiguousForward, never re-forwards; Type 2 via forward to the app
  subnet's range-top canister id (never allocated); cycles-ledger delivery
  arm against the real cycles ledger; treasury hold (cap-0 default), resume,
  73h max-wait escalation; full dedup/Type-1/refund-auto-resolve matrix.
- BLOCKER (environment, not code): IC replica memory tracker asserts
  PAGE_SIZE==4096; this sandbox VM is 16K-page aarch64 → pocket-ic crashes
  at instance creation (verified; jemalloc warning is benign, the
  memory_tracker assert is not). No KVM. qemu-user x86_64: pocket-ic
  segfaults in jemalloc init — guest mmap returns bit-47-set addresses
  (host-VA passthrough), noncanonical for x86_64 userspace; -R reserved-VA
  workaround fatally conflicts with vsyscall page alloc. Dead end, abandoned.
- CI route: wrote .github/workflows/integration.yml but the sandbox's OAuth
  token LACKS `workflow` scope (push rejected) → parked at
  test/integration/ci/integration.yml w/ install instructions in README.
- Verified here: tsc --noEmit clean, vitest lists all 15, mops check ✓,
  mops test ✓ (358), mops build ✓, icp build ✓. PRD task 12 → ◐.
NEXT: run the suite on a 4K-page host (operator: `cd test/integration &&
npm ci && npm test` on the Mac, or git mv ci/integration.yml into
.github/workflows/ and push w/ a workflow-scoped token), fix whatever the
live run reveals, flip task 12 to ☑; then task 13 (frontend M1).
## 2026-06-10 — Iteration 14 — Task 13: Frontend M1 (DONE)
Task 12 stays ◐ (blocked on 4K-page host, see memory) — task 13 was the
highest-priority actionable item.
- src/frontend/: Vite + vanilla-TS SPA. DECISION: Astro rejected (SSG
  machinery around the same Vite for one interactive page — the
  mo:server/hmac/json dependency stance). deps: @icp-sdk/auth ^7, core ^5.3;
  dev: bindgen ^0.3, vite 6, vitest 3, @types/node.
- Bindings: @icp-sdk/bindgen Vite plugin regenerates src/bindings/backend.ts
  from the COMMITTED backend.did every build (typed actor can't drift).
  .gitignore now `src/backend/dist/*` + `!…/backend.did` (spec §8 committed
  .did; mops build refreshes it → self-maintaining). NOTE bindgen wrapper
  shapes: variants are `__kind__`-discriminated unions, opt fields are
  `?: T` (undefined, NOT null) — but top-level opt RETURNS are `T | null`;
  OrderStatus is a TS string enum. Frontend types derived structurally from
  the actor (Awaited<ReturnType<…>>), immune to bindgen export naming.
- II: @icp-sdk/auth 7.x, `new AuthClient({identityProvider:
  "https://id.ai/authorize"})` — /authorize REQUIRED in 7.x (URL verbatim);
  mainnet II works against local replicas (icp-cli ≥0.2.4 trusts mainnet
  sigs) so no env branching. ic_env cookie via safeGetCanisterEnv for
  canister id + rootKey (never fetchRootKey — MITM). createActor takes
  {agentOptions}, NEVER {agent} (silent anonymous downgrade).
- UI: tier picker (card_tiers), lowFloat soft-gate notice (treasury_status),
  both Destination arms (canister / cycles-ledger acct + optional hex
  subaccount, left-padded to 32B), create_order error→message map
  (rateUnavailable = "nothing was charged", §3.1), Payment Link =
  tier.paymentLinkUrl + client_reference_id (links are session-local — the
  backend stores none; reopened created-orders show status, no pay button),
  3s get_order polling → 4-step timeline, exactly delivered/errorQueue stop
  the poll (pinned), expired stays pollable (§4 advisory), list_orders
  history w/ row-click re-poll, rail tabs = §11.1 seam (ck-USDC disabled).
- .ic-assets.json5: allow_raw_access false, standard security_policy,
  immutable assets/ caching, SPA aliasing.
- icp.yaml: frontend canister via @dfinity/asset-canister@v2.2.1 — v2.1.0
  (the skill's version) PANICS on icp-cli 0.3.2: its `assets` sync step
  type was removed; 2.2.1 uses the plugin-based sync.
- vite.config.ts gotcha: vitest runs with command==="serve" → dev-server
  config (which shells out to `icp network status`) must ALSO gate on
  mode !== "test".
- Tests: 21 vitest (terminal-set pin, step monotonicity, link ?/& cases,
  cycles/USD formatting incl. §3 vector 3_353_350_000_000 → "3.353 T",
  subaccount hex matrix, error-message distinctness). tsc --noEmit strict
  clean; vite build ✓; mops check/test (358)/build ✓; icp build ✓ both
  canisters. Live II popup + certified serving need a browser + deployed
  canister → dev-host verification alongside task 12's suite.
NEXT: task 14 — ck-USDC rail (rails/CkUsdc.mo, §6.2): ICRC-1/2 bindings,
icrc2_transfer_from pull after user approve, block_index dedup, amount-short
mismatch handling, hold-ckUSDC treasury posture; unit tests.
## 2026-06-10 — Iteration 15 — Task 14: ck-USDC rail (DONE)
Task 12 stays ◐ (blocked on 4K-page host, see memory) — task 14 was the
highest-priority actionable item.
- src/backend/rails/CkUsdc.mo (pure half): ICRC-1/2 types (self-contained,
  no Cmc import); ledger xevnm-gaaaa-aaaar-qafnq-cai pinned; 1¢ = 10^4 units
  (6 decimals + 1:1 peg ⇒ usdCents pricing exact on money-in); buildPullIntent
  = §5.1 write-intent-before-call, MONEY-IN edition (transfer_from debits the
  USER): created_at_time frozen, memo = orderId UTF-8 (exactly the 32-byte
  ICRC-1 bound, ledger-tx→order audit link); transferFromArgs pure projection
  → replay bit-identical; interpretPull encodes the DEDUP-FIRST rule
  (ic-icrc1 checks created_at_time dedup before balance/allowance, so
  InsufficientAllowance/Funds/BadFee PROVE nothing ever moved → #drop intent,
  clean user error = §6.2 amount-short handling; TooOld = #uncertain,
  escalate; TemporarilyUnavailable/CreatedInFuture/GenericError = #retry);
  pull journal (financial record, never pruned) w/ escalatedAtNs once-only
  mark; claimStage = whole resume decision pure (#created/#expired claimable,
  block-beats-escalation #recoverBlock heal, exactly-window escalates).
- Main.mo: create_ck_usdc_order(usdCents, destination) — NO tiers (nothing
  structural pins the amount like a Payment Link; canister pulls exact
  price); operator bounds FAIL CLOSED (maxUsdCents=0 default = rail
  disabled); rail fee formula default 0/0 (no processor fee; conversion cost
  operator-absorbed §3), admin-settable. claim_ck_usdc_order: owner-only (no
  existence leak), wrongRail guard, transient pullsInFlight single-flight,
  driveClaim: stage → pull → creditPull (dedup recordCkUsdcBlock + journal
  block + #paid ONE sync block) → detached processMint kick. Stale intent →
  #stuckMint{stage="stalePullIntent"}, order STAYS #created (no legal edge,
  position maybe-nothing-happened); ops levers: ck_usdc_pull (admin query),
  reset_ck_usdc_pull (refuses if block recorded), withdraw_ck_usdc (hold-
  ckUSDC posture: accrued ckUSDC → operator → off-chain →ICP float refill).
  Refactor: quoteCents (per-rail fee over shared rate cache) +
  quoteWithRefresh + createOrderWithFreshId; card create_order delegates.
- Tests: ckusdc (32) — pinned ledger id/window/units, config+amount boundary
  matrices, intent/wire-form vectors (32B memo), interpretPull matrix,
  journal patch + no-op-on-missing, claimStage all-8-status pin + window
  boundary + once-only escalation. 390 total green.
- Gotchas: `transient` is a moc 1.9 KEYWORD — illegal as a variant tag
  (#transient → syntax error M0001); renamed #retryable. `ignore async {}`
  needs send capability — NOT available in a plain (non-async*) private
  func; inline the detached kick into the async* driver instead of a helper.
- Verified: mops check ✓ (lint clean), mops test ✓ (390), mops build ✓,
  icp build ✓ both canisters; .did gains exactly the 7 ck_usdc methods;
  frontend rebuilt against new committed .did (tsc strict + vite build + 21
  vitest ✓ — bindgen regenerates, M2 UI is task 16).
NEXT: Task 15 — PocketIC suite, ck-USDC go-live bar: deploy a local ICRC-2
ledger as ck-USDC, approve/pull happy path, dedup/replay, amount-short,
stale-intent escalation, treasury interplay (same 4K-page host constraint
as task 12 — implement + typecheck here, execute on dev host/CI).
## 2026-06-10 — Iteration 16 — Task 15: PocketIC ck-USDC bar (IMPLEMENTED; run pending 4K-page host)
RESUMED IN-FLIGHT WORK: previous session left harness.ts/idl.ts/types.ts
ck-USDC halves uncommitted (icrc2 factory, ledger-init encoder, CkLedger
install/approve helpers, fiduciary subnet in setup) but no spec file, no
wasm fetch, no package wiring. Reviewed the in-flight IDL field-by-field
against the committed backend.did (exact match) and kept it.
- KEY MECHANISM: PocketIC's fiduciary subnet mirrors mainnet's canister
  range, so setupCanister({targetCanisterId}) installs the REAL
  ic-icrc1-ledger at exactly xevnm-gaaaa-aaaar-qafnq-cai — the id CkUsdc.mo
  pins. Wasm from pinned release ledger-suite-icrc-2026-03-09, sha256
  354dd6ec… verified by new scripts/fetch-ledger-wasm.mjs (binary
  gitignored, pin committed — §8 stance); init args verified against the
  wasm's own candid:service metadata via ic-wasm. pretest = fetch + mops
  build, so dev-host/CI `npm test` runs both bars unchanged.
- src/ckusdc.spec.ts: 11 scenarios, own instance. Fail-closed rail config
  (default-disabled, gated, validated, public) + bounds-before-quote;
  §3 pricing twin (500¢ @ 0/0 formula → 3.685T locked, end-to-end through
  the real transform); claim guards (anon/non-owner/unknown/wrongRail/
  no-approval); §6.2 amount-short BOTH arms (allowance short → required
  carried, intent dropped, order claimable; balance short ditto) then
  approve→pull → Paid w/ exact ledger accounting (debit/credit/allowance-
  spent/32B memo); §5.1 money-IN replay — upgrade mid-pull after intent
  journals, orphaned transfer_from executes, re-claim replays identical
  args → ledger #Duplicate → debited EXACTLY once (balances pinned);
  stale-intent via stopCanister on the ledger (deterministic never-executed
  pull) + 24h jump → once-only stalePullIntent escalation, order stays
  Created, reset_ck_usdc_pull reopens (refusal-on-settled also pinned) →
  claim → delivered; treasury interplay (cap-0 hold → size → process_order
  → delivered, one float debit, ceil-dust 25_000 cycles operator-side);
  withdraw lever (3-pull accrual − fee, over-withdraw → err); trail (tags,
  seq, zero open entries after operator resolve).
- Gotchas: vitest fileParallelism:false runs spec files sequentially —
  ckusdc gets its own instance, no card-state interference. Actor typing:
  spec-local poorUser actor needs `Actor<BackendService>` from @dfinity/pic
  (createActor's ReturnType isn't generic-bound). GitHub release URL 302 ≠
  exists-check: probe with -L download + gunzip -t + wasm magic.
- Verified: fetch script ✓ (sha256 match), tsc --noEmit ✓, vitest lists 26
  (15 card + 11 ck), mops check ✓, mops test ✓ (390), mops build ✓,
  icp build ✓, .did unchanged. PRD task 15 → ◐ (same actually-ran bar as
  task 12). Also committing the user's afk-ralph.sh tweak (usage guard +
  push step) found in the working tree.
NEXT: task 16 — Frontend M2: ck-USDC panel (amount input within public
ck_usdc_config bounds, create → icrc2_approve via wallet/ledger → claim
flow w/ claim-error→action mapping, rail tab enable); then tasks 17–18
(reproducible build + runbook). Tasks 12/15 still need the dev-host/CI run.
## 2026-06-10 — Iteration 17 — Task 16: Frontend M2 ck-USDC panel (DONE)
Tasks 12/15 stay ◐ (4K-page host, see memory) — task 16 was the highest-
priority actionable item.
- index.html restructured: rail tabs are REAL tabs now — per-rail panels
  (Card: tiers; ck: amount input + bounds line + fail-closed "not enabled"
  notice off maxUsdCents==0) feeding ONE shared destination form + submit;
  lowFloat notice hoisted above tabs (treasury hold is rail-agnostic);
  active-order gains ck-pay-area (Approve/Claim buttons + flow status) and
  a Rail row; history gains a Rail column.
- src/ledger.ts: hand-rolled minimal ICRC-2 actor (raw IDL — external
  canister, no committed .did for bindgen; 3 methods: balance_of/allowance/
  approve), ledger id pinned = CkUsdc.mo's mainnet xevnm-…-cai (PocketIC
  fiduciary subnet hosts it at the same id → no env branching); actor.ts
  agentOptions extracted+shared (ic_env host/rootKey); identity REQUIRED
  (anonymous approve burns fee from the shared anonymous account).
- Flow: create_ck_usdc_order → "1·Approve" (icrc2_approve, spender=backend,
  amount=approveUnits, II identity) auto-chains into claim; "2·Claim"
  standalone for approved-earlier/retry. RECONSTRUCTABLE cross-session
  (unlike card pay links): units = pricing.usdCents·10^4 + public config
  ledgerFee; claim's insufficientAllowance.required is ledger-authoritative
  and overrides per-order (ckRequiredUnits map). Per-order busy guard;
  renderCkPay re-renders on poll ticks w/o clobbering flow status.
- format.ts (pure, tested): ckUnitsForCents (10^4 pin), formatCkUsdcUnits
  (6dp, ≥2 shown), parseUsdAmount (strict: optional $, ≤2dp, no guessing),
  createCkOrderErrorMessage (bounds carry $ values), claimErrorInfo §6.2
  action mapping (amount-short → approve/fund + "nothing was charged";
  retryable/inFlight → retry; staleIntent/badFee/ledgerRejected/
  notClaimable/wrongRail/notFound/anonymous → "none", pinned ALL-7 sweep;
  staleIntent says "not charged twice" §5.1), approveErrorMessage (raw
  one-key variant records).
- Tests: +17 vitest (38 total). Verified: tsc --noEmit strict ✓, vite
  build ✓, mops check ✓, mops test ✓ (390), mops build ✓, icp build ✓
  both canisters, committed .did unchanged (bindings regen identical).
- Gotcha: bindgen surfaces PAYLOAD-LESS variants (Rail, OrderStatus) as TS
  string enums, NOT __kind__ unions — compare via (x as unknown as string).
NEXT: task 17 — Reproducible build + release (spec §8): Docker-pinned
build, ic-wasm deterministic optimize + metadata, committed .did (already
self-maintaining), published expected module hash per tagged release,
asset-canister certified frontend. Tasks 12/15 still need the dev-host/CI
run (live browser approve→claim rides the same session).
## 2026-06-10 — Iteration 18 — Task 17: Reproducible build + release (DONE)
Tasks 12/15 stay ◐ (4K-page host, see memory) — task 17 was the highest-
priority actionable item.
- icp.yaml backend recipe += shrink: true (ic-wasm deterministic optimize,
  877K → 834K; ALL metadata sections survive incl. motoko:stable-types +
  enhanced-orthogonal-persistence — checked, upgrades depend on them) and
  candid: src/backend/dist/backend.did (committed .did IS the embedded
  candid:service — bindgen input and deployed interface provably same bytes;
  verified by diffing the embedded section). NO compress: on-chain module
  hash stays directly sha256sum-comparable.
- Dockerfile.release: node:22.22.1-bookworm-slim BY DIGEST (multi-arch
  manifest list — per-platform resolve, caveat doc'd), libdbus-1-3 (icp-cli
  needs it), npm -g ic-mops@2.13.2 @icp-sdk/icp-cli@0.3.2
  @icp-sdk/ic-wasm@0.9.11; moc 1.9.0 rides the committed mops.toml; final
  `FROM scratch` stage so `docker build --output type=local` writes exactly
  the artifacts.
- scripts/release-build.sh (inner; rm -rf artifact cache FIRST — determinism
  re-earned every build, never assumed) → backend.wasm/frontend.wasm/
  backend.did/MODULE-HASHES.txt; scripts/reproducible-build.sh (host
  wrapper): `git archive <ref> | docker build -` — committed-tree-only
  context, dirty-tree warning; hashes are a pure function of the ref.
- RELEASE.md: pin table, cut-a-release (tag → build → publish
  MODULE-HASHES.txt → deploy → GATE on icp canister status == published
  hash), verify-a-release (`icp canister status -n ic --public` works for
  non-controllers + dashboard), frontend posture (module hash = pinned
  recipe wasm; asset CONTENT verified per-response by certified assets +
  allow_raw_access:false; dist/ rebuildable from lockfile), caveats stated
  (arch independence verified arm64-only; recipe tags not content-addressed).
- VERIFIED REPRODUCIBILITY EMPIRICALLY: warm-cache rebuild, clean-cache
  rebuild, and the full container build (fresh toolchain npm install, fresh
  mops/moc downloads) all byte-identical: backend 2cd20d4e…, frontend
  04e565b3… . Native == container on this host.
- Gotchas: `docker build -` + git-archive tar needs Dockerfile.release IN
  the context (committed — test pre-commit used a hand-built tar of
  git ls-files + new files); `icp canister status` exists in 0.3.2 w/
  --public state-tree fallback for non-controllers (don't guess dfx names).
- Verified: mops check ✓, mops test ✓ (390), mops build ✓, icp build ✓ both
  canisters, committed .did unchanged, container build E2E ✓.
NEXT: task 18 — Ops runbook (secret provisioning/rotation, error-queue
resolution incl. Stripe Dashboard refunds, float refill, burn-cap sizing/
override, ck-USDC levers, confidential-subnet checklist §7/§11.1). Tasks
12/15 still need the dev-host/CI run.
## 2026-06-10 — Iteration 19 — Task 18: Ops runbook (DONE)
Tasks 12/15 stay ◐ (4K-page host, see memory) — task 18 was the last
buildable task.
- RUNBOOK.md: written from the CODE, not the spec — every method/arg/
  default/capacity checked against Main.mo, every CLI command checked
  against installed icp-cli 0.3.2 (`icp canister settings update
  --add-controller`, NOT dfx's update-settings; `icp canister top-up`,
  NOT deposit-cycles — both were wrong in the first draft and caught by
  running --help).
- Sections: operating model (controllers = §7 allowlist, units table);
  go-live checklist = exactly the fail-closed defaults (secret, tiers,
  cap 0, ck max 0, alert, webhook endpoint w/ the two event types);
  secret provisioning/rotation (generation counter = no-read-back
  confirmation, Stripe overlap, boundary-node exposure → rotate-after-
  provision, leak procedure: cap-to-0 FIRST then rotate then reconcile
  then reset_burn_window); tiers/forex; treasury (cap sizing guidance:
  too-small delays, too-large is unbounded — start tight); ERROR-QUEUE
  TRIAGE TABLE — every Kind + every stuckMint stage → money position →
  action incl. the never-rules (never rebuild stale intent, never
  re-forward ambiguous); ck-USDC levers (stalePullIntent procedure off
  ledger memo = orderId, reset refusal semantics, withdraw as refund +
  float-conversion loop); recovery/manual kicks; monitoring cadence
  (healthy reads of the 5 status queries, audit seq gaps = drops);
  §7/§11.1 confidential-subnet checklist hardest-first (checkpoint/
  state-sync confidentiality before attestation before maturity);
  upgrade ops notes (mid-flight safe by §5.1; transient knobs reset by
  design).
- Verified: mops check ✓ (lint clean), mops test ✓ (18 files/390),
  mops build ✓, icp build ✓, .did untouched. PRD task 18 → ☑.
NEXT: PRD is complete except tasks 12/15 — both fully implemented +
typechecked, blocked ONLY on executing PocketIC on a 4K-page host
(operator: `cd test/integration && npm ci && npm test` on the Mac, or
git mv test/integration/ci/integration.yml → .github/workflows/ and push
w/ a workflow-scoped token). Nothing further is actionable in-sandbox.
